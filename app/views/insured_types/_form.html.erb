<%= form_for(@insured_type) do |f| %>
  <% if @insured_type.errors.any? %>
    <div class = "form_errors">
      <h2><%= pluralize(@insured_type.errors.count, "error") %> prohibited this insured_type from being saved:</h2>

      <ul>
      <% @insured_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class = 'form'>
	  <div class="field">
	    <%= f.label :name %><br />
	    <%= f.text_field :name, :autocomplete => "off"  %>
	  </div>
	  <div class="field">
	  	<%= f.label "Permenant" %><br />
	  	<%= f.check_box :perm %>
	  </div>	  
	  <div class="button">
	    <%= f.submit :class => 'submit' %>
	  </div>	
  </div>
<% end %>
