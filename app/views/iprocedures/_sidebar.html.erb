<div class = 'sidebar'>	
	<% if @edit || @show %>
		<%= link_to 'Delete Code', polymorphic_path([@procable, @proc]), :method => :delete %>
	<% end %>
	<%= link_to 'Back to ' + @name, polymorphic_path(@procable)%>
	<br />
</div>